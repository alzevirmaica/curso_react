// Criar um bloco que agrupa vários testes relacionados ao componente App
describe("first test app component", () => {
  it("should adds 1 + 2 to equal 3", () => {
    expect(1).toBe(1);
  });
});

export default {};
